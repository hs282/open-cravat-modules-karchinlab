Key for Civic reduced to just the ID field.  The discription, actionability score, and diseases expected columns change so fast the key 
keeps getting out of date.  This is not a great fix.  They should be checked in some way but for now the three columns have been removed
so just a basic check that it is working is done (just check the expected ID for the three test variants)